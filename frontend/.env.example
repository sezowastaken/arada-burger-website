# Used by the /admin section to call the Fastify backend. This is read by
# the browser (NEXT_PUBLIC_*), so keep it as the backend's published host
# port — works the same whether this app runs via `npm run dev` or Docker.
NEXT_PUBLIC_API_URL=http://localhost:4000
